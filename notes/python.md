## variables
	-variables store values
	-type() gives the variable type
	-python auto detects types
	-type(x) will give the type of x
## git-notes
	-git init  	  -initialises
	-git add .        -tracks all the progress
	-git commit -m "" -adds the checkpoint to your local os, memory.
	-git push	  -pushes the code to github
